# 自动化执行记录：A股每日板块资金流报告

## 2026-08-28（首次执行）
- 抓取：成功。通过 WebFetch 调东方财富 push2 接口，行业板块（m:90+t:2，total=496）。
- 关键技术点：接口 `po=-1`（升序）被拒（rc=102, data:null），故净流出榜改用末页 `pn=5`（降序末尾即最负值）。每字段用 `fid` 切换排序，`pz=100` 保证 WebFetch 不截断。共 8 次抓取（4 字段 × 净流入 pn=1 / 净流出 pn=5）。
- 散户(f84)净流出榜模型返回顺序错乱，已按原始「元」值手动重排。
- 产出：`A股资金流/日报/2026-08-28 A股板块资金流.md`（核心结论+要点+机构/主力/散户/大户四分区各 TOP10 净流入/净流出）。
- PDF：未生成（reportlab/pandoc/wkhtmltopdf 均不可用，且沙箱无外网无法 pip）。报告末尾已加提示行。
- 索引：`A股资金流索引.md` 由 Dataview `FROM "A股资金流"` 自动收录，未手动改。
- 范围：本期仅行业板块，概念/地域未补充（时间所限）。

## 2026-08-28（16:00 自动化定时执行）
- 抓取：成功。沿用 pz=100 + 单字段 fid 排序 + pn=1/pn=5 方案，8 次基础抓取覆盖 机构(f66)/主力(f62)/大户(f72)/散户(f84) 的净流入/净流出 TOP10。
- 数据校验发现并修正两处模型数值错误：① f72 净流出首抓被模型 ×10 放大（如电子 -411.25，经 f62=f66+f72 校验应为 -41.12），已重抓修正；② f84 净流出首抓取错列表前段（返回正值=净流入端），已重抓取得最负值（散户净流出端）：横向通用软件 -15.76、传媒 -15.30、锂 -9.57 等。
- 主力净流出榜：f62 字段首抓跨列 ×10 失真，改用 f66-outflow 与 f84-outflow 中已校验正确的 f62 值，按 f62 重排得到正确 TOP10（电子 -186.85、半导体 -128.30、数字芯片设计 -74.25…）。
- 产出：`A股资金流/日报/2026-08-28 A股板块资金流.md`（核心结论+要点+机构/主力/散户/大户 四分区各 TOP10；含金额与涨跌幅，跨列已交叉校验；「其他电子Ⅱ/门户网站/半导体材料」缺跨列样本处标 —）。
- PDF：未生成（reportlab/pandoc/wkhtmltopdf 均不可用，沙箱无外网）。报告末尾已加提示行。
- 索引：`A股资金流索引.md` 由 Dataview 自动收录，未手动改。
