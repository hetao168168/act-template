# 自动化执行记录：每日 A股板块资金流报告

## 2026-08-31 执行
- **触发**：定时自动化（automation-1787842422080）。
- **抓取**：WebFetch 东方财富板块资金流接口（行业板块 m:90+t:2，496 个；每页≤100，pn=1..5 翻页）。
  - 关键修正：① `f84`(散户) 排序在"选 TOP/末位"模式下易失效；② 让 WebFetch 直接"选末位12"会漏项（如大户净流出漏掉电子）。
  - 稳定做法：净流入取各主体 pn=1 首屏（按字段降序前 12）；净流出抓 pn=5 整页（~96 条）原始数据，由 Python 自行排序取最负值前 10。
- **处理**：原始值为元，Python 统一 ÷1e8 → 亿元（2 位小数）；并对每行做 `f62 ≈ f66 + f72` 交叉校验（本次全部一致，无告警）。
- **落盘**：`A股资金流/日报/2026-08-31 A股板块资金流.md`（机构/主力/散户/大户 四分区，各含净流入前/净流出前 TOP10，含亿元与涨跌幅%）。索引由 Dataview 自动收录。
- **模板说明**：用户指定的 `40-storage/42-template-模板/A股板块资金流报告.md` 实际不存在（该目录为空），改用既有 `2026-08-28 A股板块资金流.md` 结构为范本。
- **PDF**：本机 reportlab/pandoc/wkhtmltopdf 均不可用，已按规则在报告末尾追加「PDF 未生成」说明行，未中断流程。
- **结论要点**：科技/传媒链（电子、传媒、计算机、数字芯片设计）强势吸金；周期/医药/电力（有色金属、医药生物、电力设备）遭集中抛售；散户反向接盘被抛售板块，抛售机构强势的传媒/科技。
- **待办**：概念/地域板块本次未补充；后续若要 PDF 需先 `pip install reportlab` 或装 pandoc。

## 2026-09-01 执行
- **触发**：定时自动化（automation-1787842422080）。
- **抓取**：WebFetch 东方财富行业板块接口（m:90+t:2，496 个）。本次改用**更稳健的解析法**：对每个主体（机构 f66 / 主力 f62 / 大户 f72 / 散户 f84）分别做 净流入 pn=1 + 净流出 pn=5 两次抓取，并强制 WebFetch **原样返回 JSON 对象（按字段标签 f14/f3/f62/f66/f72/f84）**，而非让模型拼 Markdown 表格——彻底规避了多列表格的字段错位（上轮曾出现 农林牧渔主力值 28.76 与 2.72 互相矛盾、且 ≠机构+大户 的错位问题）。
- **处理**：Python 按字段标签解析（行元组索引 0=名,1=涨跌幅,2=f62,3=f66,4=f72,5=f84），÷1e8→亿元(2位)；`f62=f66+f72` 交叉校验全部一致。
  - 踩坑记录：初版 `FIELD` 索引误把 f3 算进偏移（机构→2 实为 f62），导致整表列错位、主力列全显 +0.00；修正为 主力=2/机构=3/大户=4/散户=5 后正常。后续维护务必确认索引。
- **落盘**：`A股资金流/日报/2026-09-01 A股板块资金流.md`（四分区各 净流入前/净流出前 TOP10，含亿元与涨跌幅%）。
- **结构范本**：仍沿用既有 `2026-08-28 A股板块资金流.md`（用户指定模板文件 `40-storage/42-template-模板/A股板块资金流报告.md` 经核实不存在，目录为空）。
- **PDF**：reportlab/pandoc/wkhtmltopdf 均不可用，已追加「PDF 未生成」说明行，流程未中断。
- **结论要点**：与 8-31 相反——机构/主力集中抛售科技电子半导体链（电子单日 机构 -167.95、主力 -248.48 亿，为绝对出货核心），资金转向农林牧渔/非银金融(券商保险)/种植业/种子/电力/公用事业/商贸零售等防御+复苏板块；散户逆势大举净买入被抛售的电子(+171.35)、半导体(+77.98)、元件(+54.01) 等，呈「机构出货、散户接盘」分化；大户相对独立，净买医药生物/汽车/非银金融，科技链上与机构同向撤离。
- **待办**：概念/地域板块未补充；PDF 待本机装 reportlab/pandoc 后自动补生成。生成脚本暂存 `D:\workBuddy\gen_astock_report_2026-09-01.py` 可复用。
